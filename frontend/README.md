# Frontend

Next.js app for the AI website generation platform.

Hard-coded `WebsiteConfig` objects are rendered by a shared `SiteRenderer`.

- `/site/barber` — Elite Cuts
- `/site/cafe` — Everest Coffee

```bash
npm install
npm run test
npm run typecheck
npm run lint
npm run build
npm run docs:typedoc
npm run dev
```
